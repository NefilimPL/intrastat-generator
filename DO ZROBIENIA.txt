- automatycznie wykrywanie słowników odpowiednich folderach jeżeli już istnieja zamiast tworzenie nowego folderu  (tylko jeżeli nie ma już folderów odpowiednich)
- poprawiono wizualnie
- gitignore
- github release z wersjonowaniem (tagi) informacja o wersji w gui) koniecznie z użyciem self hosted runner
- opcjonalnie informacja o możliwości aktuliazacji + lista dostępnych wersji programu w przypadku upublicznienia.
- informacja o repo w gui + na samym repo
- lepsze obliczanie wartości statystycznej (wartośc faktury zawiera wszystkie cen ale do klienta docelowego a nie do granicy polskiej, jakis mechanizm potrzebny do wyliczania tej kwoty)
